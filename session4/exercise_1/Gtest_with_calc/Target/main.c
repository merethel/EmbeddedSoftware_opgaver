#include <stdio.h>
#include "calc.h"

int main(){

    // Well lets stick to the gtest, to test the functions... But as you can see below, the main can also run. 
    printf("3+4 = %d\n", calc_add(3,4));
}